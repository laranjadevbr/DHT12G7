<div class="on-the-right" id="bottom-menu">
    <%- '<button type=\"submit\" class=\"' + btnClass.trim() + '\">'; %>
        <%- typeof btnTitle === 'undefined' ? getFirstUpperCase('send') : getFirstUpperCase(btnTitle); %>
    </button>
</div>